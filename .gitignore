*.pyc
*.d
generated
build
build_appkit
build_macos_swiftui
deps_macos
.vscode

# iOS / Xcode build + dependency artifacts
build_ios
build_ios_device
build_xcode
build_xcode_device
build_xcode_ios
build_xcode_mac
deps_ios
# per-build derivedData (e.g. swiftui/dd_macos, swiftui/dd_ios)
dd_macos
dd_ios
*.egg-info

# per-user Xcode UI state (should never be tracked)
xcuserdata

# scratch test structures / session transcripts at repo root
/*.cif
/*.pdb
/context*.txt
# Ignore local .claude session data (worktrees, local settings) but track
# shared, checked-in skills under .claude/skills/.
.claude/*
!.claude/skills/
.superpowers/
*-this-session-is-being-continued-*.txt
build_ios_dd/

# notarized direct-distribution build artifacts
/*.dmg
/build_dmg/
/swiftui/build_*/
build_mac_verify/
swiftui/DerivedData_iOS/
swiftui/DerivedData/
RayMol-1.3.1-portable-test.zip
